# Gradle
.gradle/
build/
app/build/
*/build/

# Local
local.properties
*.iml
.idea/
*.log
*.tmp

# Keystore but keep public one
# app/keystore/*.jks  # we keep tradescanner.jks as public for CI, ignore others
app/keystore/*.b64
app/keystore/private*

# OS
.DS_Store
Thumbs.db

# Android Studio
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
!gradle/wrapper/gradle-wrapper.jar

# Temporary
*.swp
*~

# Workflows to move are intentionally kept separate as per requirement
# .github/ is not ignored, but user will copy manually
